apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: clawhost-pg-data
  namespace: clawhost
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: clawhost-pg
  namespace: clawhost
  labels:
    app: clawhost-pg
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: clawhost-pg
  template:
    metadata:
      labels:
        app: clawhost-pg
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: clawhost
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: clawhost-secrets
                  key: db-password
          volumeMounts:
            - name: pg-data
              mountPath: /var/lib/postgresql/data
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
      volumes:
        - name: pg-data
          persistentVolumeClaim:
            claimName: clawhost-pg-data
---
apiVersion: v1
kind: Service
metadata:
  name: clawhost-pg
  namespace: clawhost
spec:
  selector:
    app: clawhost-pg
  ports:
    - port: 5432
      targetPort: 5432
